import type { User } from '../types';

export const MOCK_USER: User = {
  id: 'user-barbie',
  name: 'Barbie',
  points: 1500,
  currentTier: 'Bronze',
  nextTier: 'Gold',
  pointsToNextTier: 2000,
};
