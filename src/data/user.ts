import type { User } from '../types';

export const MOCK_USER: User = {
  id: 'user-barbie',
  name: 'Barbie',
  points: 1250,
  currentTier: 'Silver',
  nextTier: 'Gold',
  pointsToNextTier: 2500,
};
